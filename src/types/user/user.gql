type User{
    login: String!
    id: Int
    avatar_url: String
    type: String
    name: String!
    roles: Roles
}

type Roles {
    user: User!
    admin: Boolean!
}

type Query {
    user: User
    roles: Roles!
}
