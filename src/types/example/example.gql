type Cat {
  name: String!
  age: Int
}

type Person {
  name: String
  age: Int
}

extend type Query {
  myCat: [Cat]
  persons: [Person]
}

input PersonInput {
  name: String!
  age: Int
}

type Mutation {
  newPerson(person: PersonInput): Person
}